/* global confirm, redux, redux_change */

jQuery(document).ready(function () {

	//$.each('.redux-field-locked', function() {
	//    if ($(this ).parent().hasClass('redux-field-container')) {
	//        $(this).after($(this ).parent().parent().find(''));
	//    }
	//} )

});
